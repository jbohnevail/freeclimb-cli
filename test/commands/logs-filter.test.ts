/* WARNING: This file is automatically generated. Please edit the files in the /generation/tests directory. */
import { expect } from "chai"
import { runCommand } from "@oclif/test"
import nock from "nock"
import { cred } from "../../src/credentials.js"

import { data } from "../../src/commands/data.js"
import { Environment } from "../../src/environment.js"

describe("logs:filter Data Test", function () {
    const testJson = {
        message: "Response from server",
    }

    const nockServerResponse = `{
  "message": "Response from server"
}`

    it("Test all required paramaters", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Logs`, {
                pql: "userInput-pql",
            })
            .query({})
            .reply(200, testJson)
        const { stdout, stderr } = await runCommand(["logs:filter", "userInput-pql"])
        expect(stdout).to.contain(nockServerResponse)
    })

    const testJsonErrorNoSuggestion = {
        code: 2,
        message: "Method Not Allowed",
        url: "https://docs.freeclimb.com/reference/error-and-warning-dictionary#2",
        details: {},
    }

    const nockServerResponseErrorNoSuggestion = `starting test`

    it("Test Freeclimb Api error repsonce is process correctly without a suggestion", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Logs`, {
                pql: "userInput-pql",
            })
            .query({})
            .reply(500, testJsonErrorNoSuggestion)
        const { error } = await runCommand(["logs:filter", "userInput-pql"])
        expect(error?.oclif?.exit).to.equal(3)
    })

    it("Sends API requests to the base URL from an environment variable", async () => {
        const orig_FREECLIMB_CLI_BASE_URL = process.env.FREECLIMB_CLI_BASE_URL
        process.env.FREECLIMB_CLI_BASE_URL = "https://user-custom-domain.example.com/apiserver"
        nock("https://user-custom-domain.example.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Logs`, {
                pql: "userInput-pql",
            })
            .query({})
            .reply(200, testJson)
        try {
            const { stdout, stderr } = await runCommand(["logs:filter", "userInput-pql"])
            expect(stdout).to.contain(nockServerResponse)
        } finally {
            if (orig_FREECLIMB_CLI_BASE_URL === undefined) delete process.env.FREECLIMB_CLI_BASE_URL
            else process.env.FREECLIMB_CLI_BASE_URL = orig_FREECLIMB_CLI_BASE_URL
        }
    })

    const testJsonErrorWithSuggestion = {
        code: 50,
        message: "Unauthorized To Make Request",
        url: "https://docs.freeclimb.com/reference/error-and-warning-dictionary#50",
        details: {},
    }

    const nockServerResponseErrorWithSuggestion = `starting test`

    it("Test Freeclimb Api error repsonce is process correctly with a suggestion", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Logs`, {
                pql: "userInput-pql",
            })
            .query({})
            .reply(500, testJsonErrorWithSuggestion)
        const { error } = await runCommand(["logs:filter", "userInput-pql"])
        expect(error?.oclif?.exit).to.equal(3)
    })

    it("Test parse error gets triggered when there is an additional argument", async () => {
        const { error } = await runCommand(["logs:filter", "userInput-pql", "additionalArguments"])
        expect(error?.oclif?.exit).to.equal(2)
    })

    it("Test error resulting in an unreadable response", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Logs`, {
                pql: "userInput-pql",
            })
            .query({})
            .reply(200, undefined)
        const { error } = await runCommand(["logs:filter", "userInput-pql"])
        expect(error?.oclif?.exit).to.equal(3)
    })

    it("checks for a warning when pql is formated incorrectly", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Logs`, {
                pql: "userInput-pql='error'",
            })
            .query({})
            .reply(200, testJson)
        await runCommand(["logs:filter", "userInput-pql='error'"])
    })

    describe("logs:filter maxItem flag test", function () {
        const testJsonLogs = {
            message: "Response from server",
            logs: [
                {
                    logNum: 1,
                    message: "this is log one",
                },
                {
                    logNum: 2,
                    message: "this is log two",
                },
                {
                    logNum: 3,
                    message: "this is log three",
                },
            ],
        }

        const nockServerResponseLogs = `[
  {
    "logNum": 1,
    "message": "this is log one"
  },
  {
    "logNum": 2,
    "message": "this is log two"
  }
]`

        const testJsonNextLogs = {
            total: 1,
            start: 0,
            end: 0,
            page: 1,
            numPages: 1,
            pageSize: 100,
            nextPageUri: null,
            logs: [
                {
                    logNum: 1,
                    message: "this is log one",
                },
                {
                    logNum: 2,
                    message: "this is log two",
                },
                {
                    logNum: 3,
                    message: "this is log three",
                },
            ],
        }

        const nockServerResponseNextLogs = `[
  {
    "logNum": 1,
    "message": "this is log one"
  },
  {
    "logNum": 2,
    "message": "this is log two"
  }
]`

        it("Test all required paramaters and flag maxItem", async () => {
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Logs`, {
                    pql: "userInput-pql",
                })
                .query({})
                .reply(200, testJsonLogs)
            const { stdout, stderr } = await runCommand([
                "logs:filter",
                "userInput-pql",
                "--maxItem",
                "2",
            ])
            expect(stdout).to.contain(nockServerResponseLogs)
        })

        it("Test maxItem with --next", async () => {
            const orig_FREECLIMB_LOGS_FILTER_NEXT = process.env.FREECLIMB_LOGS_FILTER_NEXT
            process.env.FREECLIMB_LOGS_FILTER_NEXT = "6c6f67733a66696c746572"
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Logs`)
                .query({ cursor: "6c6f67733a66696c746572" })
                .reply(200, testJsonNextLogs)
            try {
                const { stdout, stderr } = await runCommand([
                    "logs:filter",
                    "userInput-pql",
                    "--maxItem",
                    "2",
                    "--next",
                ])
                expect(stdout).to.contain(nockServerResponseNextLogs)
            } finally {
                if (orig_FREECLIMB_LOGS_FILTER_NEXT === undefined)
                    delete process.env.FREECLIMB_LOGS_FILTER_NEXT
                else process.env.FREECLIMB_LOGS_FILTER_NEXT = orig_FREECLIMB_LOGS_FILTER_NEXT
            }
        })
    })

    describe("logs:filter next flag test", function () {
        it("Tests return of Exit Code 3 when flag next is not available", async () => {
            const orig_FREECLIMB_LOGS_FILTER_NEXT = process.env.FREECLIMB_LOGS_FILTER_NEXT
            delete process.env.FREECLIMB_LOGS_FILTER_NEXT
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Logs`, {
                    pql: "userInput-pql",
                })
                .query({})
                .reply(200, testJson)
            try {
                await runCommand(["logs:filter", "userInput-pql"])
                const { error } = await runCommand(["logs:filter", "userInput-pql", "--next"])
                expect(error?.oclif?.exit).to.equal(3)
            } finally {
                if (orig_FREECLIMB_LOGS_FILTER_NEXT === undefined)
                    delete process.env.FREECLIMB_LOGS_FILTER_NEXT
                else process.env.FREECLIMB_LOGS_FILTER_NEXT = orig_FREECLIMB_LOGS_FILTER_NEXT
            }
        })

        const testJsonNext = {
            total: 1,
            start: 0,
            end: 0,
            page: 1,
            numPages: 1,
            pageSize: 100,
            nextPageUri: null,
        }

        const nockServerResponseNext = `== You are on the last page of output. ==`

        const testJsonNext2 = {
            total: 2,
            start: 0,
            end: 0,
            page: 1,
            numPages: 2,
            pageSize: 100,
            nextPageUri: "",
        }

        const nockServerResponseNext2 = `== Currently on page 1. Run this command again with the -n flag to go to the next page. ==`

        it("Test flag next works as expected when available with on last page", async () => {
            const orig_FREECLIMB_LOGS_FILTER_NEXT = process.env.FREECLIMB_LOGS_FILTER_NEXT
            process.env.FREECLIMB_LOGS_FILTER_NEXT = "6c6f67733a66696c746572"
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Logs`)
                .query({ cursor: "6c6f67733a66696c746572" })
                .reply(200, testJsonNext)
            try {
                const { stdout, stderr } = await runCommand([
                    "logs:filter",
                    "userInput-pql",
                    "--next",
                ])
                expect(stdout).to.contain(nockServerResponseNext)
            } finally {
                if (orig_FREECLIMB_LOGS_FILTER_NEXT === undefined)
                    delete process.env.FREECLIMB_LOGS_FILTER_NEXT
                else process.env.FREECLIMB_LOGS_FILTER_NEXT = orig_FREECLIMB_LOGS_FILTER_NEXT
            }
        })

        const finalCursor = "freeClimbCLIAutomatedTestCursor"

        before(() => {
            ;(async () => {
                testJsonNext2.nextPageUri = `https://www.freeclimb.com/apiserver/Accounts/${await cred.accountId}/Logs?cursor=${finalCursor}`
            })()
        })
        after(() => {
            const dataDir = data.run([]).then(
                (dataDir) => {
                    const environment = new Environment(dataDir)
                    environment.clearString("FREECLIMB_LOGS_FILTER_NEXT")
                },
                (reason) => console.log(reason),
            )
        })
        it("Test flag next works as expected when available with has more pages", async () => {
            const orig_FREECLIMB_LOGS_FILTER_NEXT = process.env.FREECLIMB_LOGS_FILTER_NEXT
            process.env.FREECLIMB_LOGS_FILTER_NEXT = "6c6f67733a66696c746572"
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Logs`)
                .query({ cursor: "6c6f67733a66696c746572" })
                .reply(200, testJsonNext2)
            try {
                const { stdout, stderr } = await runCommand([
                    "logs:filter",
                    "userInput-pql",
                    "--next",
                ])
                expect(stdout).to.contain(nockServerResponseNext2)
            } finally {
                if (orig_FREECLIMB_LOGS_FILTER_NEXT === undefined)
                    delete process.env.FREECLIMB_LOGS_FILTER_NEXT
                else process.env.FREECLIMB_LOGS_FILTER_NEXT = orig_FREECLIMB_LOGS_FILTER_NEXT
            }
        })

        it("Test error is caught when when using next flag and no reponse is given", async () => {
            const orig_FREECLIMB_LOGS_FILTER_NEXT = process.env.FREECLIMB_LOGS_FILTER_NEXT
            process.env.FREECLIMB_LOGS_FILTER_NEXT = "6c6f67733a66696c746572"
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Logs`)
                .query({ cursor: "6c6f67733a66696c746572" })
                .reply(200, undefined)
            try {
                const { error } = await runCommand(["logs:filter", "userInput-pql", "--next"])
                expect(error?.oclif?.exit).to.equal(3)
            } finally {
                if (orig_FREECLIMB_LOGS_FILTER_NEXT === undefined)
                    delete process.env.FREECLIMB_LOGS_FILTER_NEXT
                else process.env.FREECLIMB_LOGS_FILTER_NEXT = orig_FREECLIMB_LOGS_FILTER_NEXT
            }
        })
    })
})

describe("logs:filter Status Test", function () {
    const testJsonStatus = ""

    const statusResponse = `Received a success code from FreeClimb. There is no further output.
`

    it("Test all required paramaters", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Logs`, {
                pql: "userInput-pql",
            })
            .query({})
            .reply(204, testJsonStatus)
        const { stdout, stderr } = await runCommand(["logs:filter", "userInput-pql"])
        expect(stdout).to.contain(statusResponse)
    })
})
