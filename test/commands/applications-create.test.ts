/* WARNING: This file is automatically generated. Please edit the files in the /generation/tests directory. */
import { expect } from "chai"
import nock from "nock"
import { runCommand } from "@oclif/test"
import { cred } from "../../src/credentials"

describe("applications:create Data Test", function () {
    const testJson = {
        message: "Response from server",
    }

    const nockServerResponse = `{
  "message": "Response from server"
}`

    afterEach(() => {
        nock.cleanAll()
    })

    it("Test all required paramaters", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {})
            .query({})
            .reply(200, testJson)
        const { stdout } = await runCommand(["applications:create"])
        expect(stdout).to.contain(nockServerResponse)
    })

    it("Test Freeclimb Api error repsonce is process correctly without a suggestion", async () => {
        const testJsonErrorNoSuggestion = {
            code: 2,
            message: "Method Not Allowed",
            url: "https://docs.freeclimb.com/reference/error-and-warning-dictionary#2",
            details: {},
        }
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {})
            .query({})
            .reply(500, testJsonErrorNoSuggestion)
        const { error } = await runCommand(["applications:create"])
        expect(error?.oclif?.exit).to.equal(3)
    })

    it("Sends API requests to the base URL from an environment variable", async () => {
        nock("https://user-custom-domain.example.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {})
            .query({})
            .reply(200, testJson)
        const orig = process.env.FREECLIMB_CLI_BASE_URL
        process.env.FREECLIMB_CLI_BASE_URL = "https://user-custom-domain.example.com/apiserver"
        try {
            const { stdout } = await runCommand(["applications:create"])
            expect(stdout).to.contain(nockServerResponse)
        } finally {
            if (orig !== undefined) process.env.FREECLIMB_CLI_BASE_URL = orig
            else delete process.env.FREECLIMB_CLI_BASE_URL
        }
    })

    it("Test Freeclimb Api error repsonce is process correctly with a suggestion", async () => {
        const testJsonErrorWithSuggestion = {
            code: 50,
            message: "Unauthorized To Make Request",
            url: "https://docs.freeclimb.com/reference/error-and-warning-dictionary#50",
            details: {},
        }
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {})
            .query({})
            .reply(500, testJsonErrorWithSuggestion)
        const { error } = await runCommand(["applications:create"])
        expect(error?.oclif?.exit).to.equal(3)
    })

    it("Test parse error gets triggered when there is an additional argument", async () => {
        const { error } = await runCommand(["applications:create", "additionalArguments"])
        expect(error?.oclif?.exit).to.equal(2)
    })

    it("Test error resulting in an unreadable response", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {})
            .query({})
            .reply(200, undefined)
        const { error } = await runCommand(["applications:create"])
        expect(error?.oclif?.exit).to.equal(3)
    })

    it("testing all body parameters together and required query are sent through with request", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {
                alias: "userInput-alias",
                voiceUrl: "userInput-voiceUrl",
                voiceFallbackUrl: "userInput-voiceFallbackUrl",
                callConnectUrl: "userInput-callConnectUrl",
                statusCallbackUrl: "userInput-statusCallbackUrl",
                smsUrl: "userInput-smsUrl",
                smsFallbackUrl: "userInput-smsFallbackUrl",
            })
            .query({})
            .reply(200, testJson)
        const { stdout } = await runCommand([
            "applications:create",
            "--alias",
            "userInput-alias",
            "--voiceUrl",
            "userInput-voiceUrl",
            "--voiceFallbackUrl",
            "userInput-voiceFallbackUrl",
            "--callConnectUrl",
            "userInput-callConnectUrl",
            "--statusCallbackUrl",
            "userInput-statusCallbackUrl",
            "--smsUrl",
            "userInput-smsUrl",
            "--smsFallbackUrl",
            "userInput-smsFallbackUrl",
        ])
        expect(stdout).to.contain(nockServerResponse)
    })

    describe("applications:create body param flags", function () {
        afterEach(() => {
            nock.cleanAll()
        })

        it("required params and a body param is sent through with request-alias", async () => {
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {
                    alias: "userInput-alias",
                })
                .query({})
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "applications:create",
                "--alias",
                "userInput-alias",
            ])
            expect(stdout).to.contain(nockServerResponse)
        })

        it("required params and a body param is sent through with request-voiceUrl", async () => {
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {
                    voiceUrl: "userInput-voiceUrl",
                })
                .query({})
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "applications:create",
                "--voiceUrl",
                "userInput-voiceUrl",
            ])
            expect(stdout).to.contain(nockServerResponse)
        })

        it("required params and a body param is sent through with request-voiceFallbackUrl", async () => {
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {
                    voiceFallbackUrl: "userInput-voiceFallbackUrl",
                })
                .query({})
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "applications:create",
                "--voiceFallbackUrl",
                "userInput-voiceFallbackUrl",
            ])
            expect(stdout).to.contain(nockServerResponse)
        })

        it("required params and a body param is sent through with request-callConnectUrl", async () => {
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {
                    callConnectUrl: "userInput-callConnectUrl",
                })
                .query({})
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "applications:create",
                "--callConnectUrl",
                "userInput-callConnectUrl",
            ])
            expect(stdout).to.contain(nockServerResponse)
        })

        it("required params and a body param is sent through with request-statusCallbackUrl", async () => {
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {
                    statusCallbackUrl: "userInput-statusCallbackUrl",
                })
                .query({})
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "applications:create",
                "--statusCallbackUrl",
                "userInput-statusCallbackUrl",
            ])
            expect(stdout).to.contain(nockServerResponse)
        })

        it("required params and a body param is sent through with request-smsUrl", async () => {
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {
                    smsUrl: "userInput-smsUrl",
                })
                .query({})
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "applications:create",
                "--smsUrl",
                "userInput-smsUrl",
            ])
            expect(stdout).to.contain(nockServerResponse)
        })

        it("required params and a body param is sent through with request-smsFallbackUrl", async () => {
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {
                    smsFallbackUrl: "userInput-smsFallbackUrl",
                })
                .query({})
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "applications:create",
                "--smsFallbackUrl",
                "userInput-smsFallbackUrl",
            ])
            expect(stdout).to.contain(nockServerResponse)
        })
    })

    describe("applications:create next flag test", function () {
        afterEach(() => {
            nock.cleanAll()
        })

        it("Tests return of Exit Code 3 when flag next is not available", async () => {
            nock("https://www.freeclimb.com")
                .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {})
                .query({})
                .reply(200, testJson)
            const orig = process.env.FREECLIMB_APPLICATIONS_CREATE_NEXT
            process.env.FREECLIMB_APPLICATIONS_CREATE_NEXT = undefined
            try {
                await runCommand(["applications:create"])
                const { error } = await runCommand(["applications:create", "--next"])
                expect(error?.oclif?.exit).to.equal(3)
            } finally {
                if (orig !== undefined) process.env.FREECLIMB_APPLICATIONS_CREATE_NEXT = orig
                else delete process.env.FREECLIMB_APPLICATIONS_CREATE_NEXT
            }
        })
    })
})

describe("applications:create Status Test", function () {
    const testJsonStatus = ""
    const statusResponse = `Received a success code from FreeClimb. There is no further output.
`

    afterEach(() => {
        nock.cleanAll()
    })

    it("Test all required paramaters", async () => {
        nock("https://www.freeclimb.com")
            .post(`/apiserver/Accounts/${await cred.accountId}/Applications`, {})
            .query({})
            .reply(204, testJsonStatus)
        const { stdout } = await runCommand(["applications:create"])
        expect(stdout).to.contain(statusResponse)
    })
})
