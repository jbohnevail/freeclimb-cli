/* WARNING: This file is automatically generated. Please edit the files in the /generation/tests directory. */
import { expect } from "chai"
import nock from "nock"
import { runCommand } from "@oclif/test"
import { cred } from "../../src/credentials"

describe("calls:list Data Test", function () {
    const testJson = {
        message: "Response from server",
    }

    afterEach(() => {
        nock.cleanAll()
    })

    it("Test all required paramaters", async () => {
        nock("https://www.freeclimb.com")
            .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
            .query({})
            .reply(200, testJson)
        const { stdout } = await runCommand(["calls:list", "--json"])
        expect(stdout).to.contain('"message": "Response from server"')
    })

    it("Test Freeclimb Api error repsonce is process correctly without a suggestion", async () => {
        const testJsonErrorNoSuggestion = {
            code: 2,
            message: "Method Not Allowed",
            url: "https://docs.freeclimb.com/reference/error-and-warning-dictionary#2",
            details: {},
        }
        nock("https://www.freeclimb.com")
            .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
            .query({})
            .reply(500, testJsonErrorNoSuggestion)
        const { error } = await runCommand(["calls:list"])
        expect(error?.oclif?.exit).to.equal(3)
    })

    it("Sends API requests to the base URL from an environment variable", async () => {
        nock("https://user-custom-domain.example.com")
            .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
            .query({})
            .reply(200, testJson)
        const orig = process.env.FREECLIMB_CLI_BASE_URL
        process.env.FREECLIMB_CLI_BASE_URL = "https://user-custom-domain.example.com/apiserver"
        try {
            const { stdout } = await runCommand(["calls:list", "--json"])
            expect(stdout).to.contain('"message": "Response from server"')
        } finally {
            if (orig !== undefined) process.env.FREECLIMB_CLI_BASE_URL = orig
            else delete process.env.FREECLIMB_CLI_BASE_URL
        }
    })

    it("Test Freeclimb Api error repsonce is process correctly with a suggestion", async () => {
        const testJsonErrorWithSuggestion = {
            code: 50,
            message: "Unauthorized To Make Request",
            url: "https://docs.freeclimb.com/reference/error-and-warning-dictionary#50",
            details: {},
        }
        nock("https://www.freeclimb.com")
            .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
            .query({})
            .reply(500, testJsonErrorWithSuggestion)
        const { error } = await runCommand(["calls:list"])
        expect(error?.oclif?.exit).to.equal(3)
    })

    it("Test parse error gets triggered when there is an additional argument", async () => {
        const { error } = await runCommand(["calls:list", "additionalArguments"])
        expect(error?.oclif?.exit).to.equal(2)
    })

    it("Test error resulting in an unreadable response", async () => {
        nock("https://www.freeclimb.com")
            .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
            .query({})
            .reply(200, undefined)
        const { error } = await runCommand(["calls:list"])
        expect(error?.oclif?.exit).to.equal(3)
    })

    it("testing all query parameters and required body are sent through with request", async () => {
        nock("https://www.freeclimb.com")
            .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
            .query({
                to: "userInput-to",
                from: "userInput-from",
                status: "userInput-status",
                startTime: "userInput-startTime",
                endTime: "userInput-endTime",
                parentCallId: "userInput-parentCallId",
                active: true,
            })
            .reply(200, testJson)
        const { stdout } = await runCommand([
            "calls:list",
            "--to",
            "userInput-to",
            "--from",
            "userInput-from",
            "--status",
            "userInput-status",
            "--startTime",
            "userInput-startTime",
            "--endTime",
            "userInput-endTime",
            "--parentCallId",
            "userInput-parentCallId",
            "--active",
            "true",
            "--json",
        ])
        expect(stdout).to.contain('"message": "Response from server"')
    })

    describe("calls:list query param flags", function () {
        afterEach(() => {
            nock.cleanAll()
        })

        it("required params and a query param is sent through with request-to", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ to: "userInput-to" })
                .reply(200, testJson)
            const { stdout } = await runCommand(["calls:list", "--to", "userInput-to", "--json"])
            expect(stdout).to.contain('"message": "Response from server"')
        })

        it("required params and a query param is sent through with request-from", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ from: "userInput-from" })
                .reply(200, testJson)
            const { stdout } = await runCommand(["calls:list", "--from", "userInput-from", "--json"])
            expect(stdout).to.contain('"message": "Response from server"')
        })

        it("required params and a query param is sent through with request-status", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ status: "userInput-status" })
                .reply(200, testJson)
            const { stdout } = await runCommand(["calls:list", "--status", "userInput-status", "--json"])
            expect(stdout).to.contain('"message": "Response from server"')
        })

        it("required params and a query param is sent through with request-startTime", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ startTime: "userInput-startTime" })
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "calls:list",
                "--startTime",
                "userInput-startTime",
                "--json",
            ])
            expect(stdout).to.contain('"message": "Response from server"')
        })

        it("required params and a query param is sent through with request-endTime", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ endTime: "userInput-endTime" })
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "calls:list",
                "--endTime",
                "userInput-endTime",
                "--json",
            ])
            expect(stdout).to.contain('"message": "Response from server"')
        })

        it("required params and a query param is sent through with request-parentCallId", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ parentCallId: "userInput-parentCallId" })
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "calls:list",
                "--parentCallId",
                "userInput-parentCallId",
                "--json",
            ])
            expect(stdout).to.contain('"message": "Response from server"')
        })

        it("required params and a query param is sent through with request-active", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ active: true })
                .reply(200, testJson)
            const { stdout } = await runCommand(["calls:list", "--active", "true", "--json"])
            expect(stdout).to.contain('"message": "Response from server"')
        })
    })

    describe("calls:list next flag test", function () {
        const testJsonNext = {
            total: 1,
            start: 0,
            end: 0,
            page: 1,
            numPages: 1,
            pageSize: 100,
            nextPageUri: null,
        }

        const testJsonNext2 = {
            total: 2,
            start: 0,
            end: 0,
            page: 1,
            numPages: 2,
            pageSize: 100,
            nextPageUri: "",
        }

        before(async () => {
            const accountId = await cred.accountId
            testJsonNext2.nextPageUri = `https://www.freeclimb.com/apiserver/Accounts/${accountId}/Calls?cursor=freeClimbCLIAutomatedTestCursor`
        })

        afterEach(() => {
            nock.cleanAll()
        })

        it("Tests return of Exit Code 3 when flag next is not available", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({})
                .reply(200, testJson)
            const orig = process.env.FREECLIMB_CALLS_LIST_NEXT
            delete process.env.FREECLIMB_CALLS_LIST_NEXT
            try {
                await runCommand(["calls:list", "--json"])
                const { error } = await runCommand(["calls:list", "--next"])
                expect(error?.oclif?.exit).to.equal(3)
            } finally {
                if (orig !== undefined) process.env.FREECLIMB_CALLS_LIST_NEXT = orig
                else delete process.env.FREECLIMB_CALLS_LIST_NEXT
            }
        })

        it("Test flag next works as expected when available with on last page", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ cursor: "63616c6c733a6c697374" })
                .reply(200, testJsonNext)
            const orig = process.env.FREECLIMB_CALLS_LIST_NEXT
            process.env.FREECLIMB_CALLS_LIST_NEXT = "63616c6c733a6c697374"
            try {
                const { stdout } = await runCommand(["calls:list", "--next", "--json"])
                expect(stdout).to.contain('"page": 1')
            } finally {
                if (orig !== undefined) process.env.FREECLIMB_CALLS_LIST_NEXT = orig
                else delete process.env.FREECLIMB_CALLS_LIST_NEXT
            }
        })

        it("Test flag next works as expected when available with has more pages", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ cursor: "63616c6c733a6c697374" })
                .reply(200, testJsonNext2)
            const orig = process.env.FREECLIMB_CALLS_LIST_NEXT
            process.env.FREECLIMB_CALLS_LIST_NEXT = "63616c6c733a6c697374"
            try {
                const { stdout } = await runCommand(["calls:list", "--next", "--json"])
                expect(stdout).to.contain('"page": 1')
            } finally {
                if (orig !== undefined) process.env.FREECLIMB_CALLS_LIST_NEXT = orig
                else delete process.env.FREECLIMB_CALLS_LIST_NEXT
            }
        })

        it("Test error is caught when when using next flag and no reponse is given", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({ cursor: "63616c6c733a6c697374" })
                .reply(200, undefined)
            const orig = process.env.FREECLIMB_CALLS_LIST_NEXT
            process.env.FREECLIMB_CALLS_LIST_NEXT = "63616c6c733a6c697374"
            try {
                const { error } = await runCommand(["calls:list", "--next"])
                expect(error?.oclif?.exit).to.equal(3)
            } finally {
                if (orig !== undefined) process.env.FREECLIMB_CALLS_LIST_NEXT = orig
                else delete process.env.FREECLIMB_CALLS_LIST_NEXT
            }
        })
    })

    describe("calls:list boolean input test", function () {
        afterEach(() => {
            nock.cleanAll()
        })

        it("tests that value false can be used with boolean flags and args", async () => {
            nock("https://www.freeclimb.com")
                .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
                .query({
                    to: "userInput-to",
                    from: "userInput-from",
                    status: "userInput-status",
                    startTime: "userInput-startTime",
                    endTime: "userInput-endTime",
                    parentCallId: "userInput-parentCallId",
                    active: false,
                })
                .reply(200, testJson)
            const { stdout } = await runCommand([
                "calls:list",
                "--to",
                "userInput-to",
                "--from",
                "userInput-from",
                "--status",
                "userInput-status",
                "--startTime",
                "userInput-startTime",
                "--endTime",
                "userInput-endTime",
                "--parentCallId",
                "userInput-parentCallId",
                "--active",
                "false",
                "--json",
            ])
            expect(stdout).to.contain('"message": "Response from server"')
        })

        it("tests incorrect active input results in exit code 2", async () => {
            const { error } = await runCommand(["calls:list", "--active", "flse"])
            expect(error?.oclif?.exit).to.equal(2)
        })
    })
})

describe("calls:list Status Test", function () {
    const testJsonStatus = ""
    afterEach(() => {
        nock.cleanAll()
    })

    it("Test all required paramaters", async () => {
        nock("https://www.freeclimb.com")
            .get(`/apiserver/Accounts/${await cred.accountId}/Calls`)
            .query({})
            .reply(204, testJsonStatus)
        const { stdout } = await runCommand(["calls:list", "--json"])
        expect(stdout).to.contain('"success": true')
    })
})
